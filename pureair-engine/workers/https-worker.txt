@name pureair-engine/workers/https-worker

function https_caller() {
#HTTPS Worker Script
#ifdef httpClk()
    if(httpClk()){
        if(httpData()!=""){
            #print(httpData())
            
        }else{
            tsprint("info","Could not find the version info online.")
        }
    }
#endif
}
